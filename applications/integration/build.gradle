apply from: "../../kotlin.gradle"

test {
    dependsOn(":applications:api:shadowJar")
}

dependencies {
    testCompile project(":components:rest-support")
    testCompile project(":components:jetty-support")
    testCompile "net.code-story:simplelenium:2.2"
}
